# DeadMaze 游戏自动化 — 视频讲解文稿

> 语速建议：中等偏慢，关键操作处停顿 2 秒。总时长约 8-10 分钟。


## 片头 (15s)

大家好，今天给大家介绍 DeadMaze 游戏自动化工具。

这是一个为 2.5D 俯视角打僵尸游戏 DeadMaze 构建的全自动 AI 系统。

它能做什么？自动寻路、智能战斗、补给返航，全部后台运行，不影响你干其他事。

目前支持 MazonAcademy 地图，也提供了完整的自建图工具。

开源免费，GitHub 下载即用。下面我带大家从零开始跑起来。


## 第一步：下载安装 (45s)

打开项目网站 blog.219882.xyz/deadmaze，点击页面顶部的 "下载 ZIP" 按钮。

解压到你喜欢的目录，建议放在 E 盘 Project 文件夹下。

解压后先双击 setup.bat。这个脚本会自动检测你电脑上的 Python 环境，然后用 pip 安装所有需要的依赖库，包括 OpenCV、EasyOCR、YOLO 等等。

如果还没有 Python，先去 python.org 下载安装，记得勾选 "Add Python to PATH"。

安装完成后会提示你配置 OBS Studio。

OBS 是必须的——游戏画面通过 OBS 虚拟摄像头采集，所以分辨率必须设为 1920x1080。

打开 OBS，添加游戏窗口或显示器采集，然后点菜单栏的工具 → 虚拟摄像头 → 启动。

两个依赖都就绪后，第一步就完成了。


## 第二步：配置与标定 (90s)

接着双击 run_config.bat。它会启动网页配置服务，等待 6 秒左右会自动打开浏览器。

你会看到两个页面：配置面板和标定中心。

在配置面板中，先设置游戏路径——就是你的 DeadMaze.exe 在哪个文件夹。

如果有 PushPlus token 也可以填上，这样导航停止时会收到微信通知，没有的话跳过就行。

重点在标定页面。

页面顶部有个摄像头选择器，确保选中的是 OBS 虚拟摄像头，点测试按钮确认画面正常。

然后点 "实时预览"，画面每两秒自动刷新，同时右侧会显示各个 ROI 区域的识别结果。

右侧列表列出了所有需要标定的区域：六个 OCR 状态栏——经验值、饱食度、口渴度、体力、威胁值，还有一个火堆"开"字检测。下面还有血量条、武器槽、背包和食物区域。

标定方法很简单：点击右侧某个卡片选中它，图上对应的彩色框会高亮，然后你可以直接拖拽这个框到正确位置，或者用键盘 WASD 方向键微调，按住 Shift 加速移动。

调好之后，看右侧的识别结果是不是和游戏里显示的一致。不一致就继续微调，直到数字对上。

全部调好后，点保存。标定就完成了。


## 第三步：启动导航 (60s)

双击 run_navigator.bat。

它会自动扫描 map 文件夹下所有可用的地图。MazonAcademy 已经标定好可以直接用，直接回车选择。

然后会弹出导航窗口，显示拼接好的大地图。

操作很简单：左键点击你当前在地图上的位置设定起点，右键点击你想去的地方设定终点，蓝色的线就是 A* 算法自动规划的路径。按 Enter 键，角色就开始走了。

如果需要设置多个巡逻点，连续右键点击即可，按 M 键开启循环巡逻，角色会在这些点之间来回巡逻，每个点都会停下来战斗。

途中可以按空格暂停，按 Esc 停止，按 H 手动返航到火堆，按 Q 完全退出。


## 第四步：战斗系统 (45s)

导航启动后，战斗全自动进行。

规则引擎按优先级执行：

最高优先级是补血——血量低于 80% 时自动释放技能2，前提是你技能栏第二格放的是治疗技能。

血量低于 20% 会脱战逃跑，退到安全的途径点。

威胁值大于等于 2 说明周围僵尸太多，自动返航到火堆补给。

武器空了也会自动检测——每 15 秒检查一次武器槽颜色，如果发现空了，角色会自动返回火堆并停止程序，同时给你发微信通知。

然后是饱食度、口渴度、耐力。任何一个低于阈值——默认是 15——也会触发自动返航补给。

补给流程是全自动的：回到火堆、YOLO 识别火堆位置、后台点击、OCR 确认"开"字进入交互、扫描背包 8 格找出食物和水、计算最优组合吃掉、然后离开火堆继续巡逻。

所有这些参数——补血阈值、脱战阈值、返航阈值、技能冷却、武器容差——全部可以在网页配置面板中实时修改，保存后导航器会自动读取。


## 第五步：自建地图 (90s)

如果你想在其他地图使用，需要三步。

第一步，光流法建图。运行 map_stitcher.py，指定输出文件名，比如 SunsetMall.jpg。

游戏里操纵角色沿着地图边界走一圈，再蛇形填充内部区域。脚本会通过 ORB 特征匹配自动拼接每一帧，按 T 调整裁剪框排除 HUD，按 A 开启自动模式，走完按 S 保存。

第二步，可达区标定。运行 reachability_map.py 打开你刚拼好的地图。

三种模式：默认是涂刷模式，左键涂白色代表可以走，右键涂黑色代表不能走。按 P 进入描边模式，画多边形快速填充大面积区域。按 D 进入门标记模式，在门的位置标记并设置方向。

右键点击地图上的火堆位置保存坐标。按 C 可以自动用 HSV 分割生成初稿，省去不少功夫。

第三步，导航验证。用你新建的可达图和地图启动导航，在地图上设起点终点跑一遍，看看路径是否通畅。不通的地方回到第二步修正，通常两三遍就能调好。


## 第六步：提交地图 (30s)

建好的地图欢迎提交到公共库，让其他玩家也能用。

在项目网站点击 "提交地图"，按照指引操作：确保地图文件夹按要求命名，包含 jpg 地图、png 可达图、json 火堆和门标记。

打包成以地图名、用户名、版本号命名的 ZIP 文件。

发送邮件到指定地址，标题格式是 [DeadMaze提交] 地图名 - 用户名 - 版本号。

审核通过后你的地图会出现在下载页面，署上你的名字。


## 结尾 (15s)

以上就是 DeadMaze 游戏自动化工具的完整使用教程。

项目完全开源，GitHub 链接在视频简介里。

有问题可以提 Issue，也欢迎提交你的地图和代码改进。

谢谢观看，游戏愉快！


---

### 录制建议

| 时间段 | 画面内容 |
|--------|---------|
| 片头 | 网站首页截图 |
| 第一步 | setup.bat 运行 + OBS 设置截图 |
| 第二步 | 标定页面实时预览实操 |
| 第三步 | 导航窗口选起点终点跑起来 |
| 第四步 | 战斗状态面板 / 补给流程 |
| 第五步 | map_stitcher 拼接过程 + reachability_map 涂刷 |
| 第六步 | submit 页面截图 |
| 结尾 | GitHub 仓库页面 |
